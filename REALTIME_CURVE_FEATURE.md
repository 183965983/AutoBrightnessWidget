# 实时亮度曲线功能说明

## 功能概述

本次更新添加了实时亮度曲线显示功能，以及增强的采样控制和曝光参数调整能力。

## 新增功能

### 1. 实时亮度曲线显示

- **功能描述**：在主窗口中显示摄像头亮度和屏幕亮度随时间变化的实时曲线图
- **用户界面**：在"实时亮度曲线"组框中显示
- **曲线说明**：
  - 蓝色线条：摄像头亮度（环境光亮度）
  - 橙色线条：屏幕亮度（调整后的亮度）
- **数据存储**：默认保存最近100个数据点
- **更新频率**：根据采样频率实时更新

### 2. 采样频率调整（对数刻度）

- **调整范围**：0.01Hz 到 60Hz
- **刻度类型**：对数刻度（Logarithmic Scale）
- **用户界面**：采样频率滑块（samplingFreqSlider）
- **显示格式**：
  - 小于 1Hz：显示为毫赫兹（mHz）
  - 大于等于 1Hz：显示为赫兹（Hz）
- **计算公式**：
  ```
  滑块值: 0-100
  对数频率 = -2.0 + (滑块值/100) × (1.778 - (-2.0))
  实际频率 = 10^对数频率 Hz
  采样间隔 = 1000 / 实际频率 毫秒
  ```

### 3. 曝光增益控制

- **调整范围**：0 到 255
- **用户界面**：曝光增益滑块（gainSlider）
- **功能说明**：控制摄像头的增益参数，影响图像传感器的灵敏度
- **使用场景**：
  - 在低光环境下提高增益以获得更明亮的图像
  - 在高光环境下降低增益以避免过曝
  - 配合曝光时间使用以实现最佳图像质量

### 4. 曝光时间与采样频率联动

- **联动机制**：当调整采样频率时，系统会自动检查曝光时间是否合适
- **建议规则**：曝光时间应不超过采样间隔的50%，以避免冲突
- **实现方式**：
  - 在 `updateExposureLimits()` 函数中计算最大推荐曝光时间
  - 在调试日志中输出建议信息
  - 用户可根据日志提示手动调整曝光时间

## 技术实现

### BrightnessCurveWidget 类

**文件位置**：`BrightnessCurveWidget.h` / `BrightnessCurveWidget.cpp`

**主要方法**：
- `addDataPoint(int cameraBrightness, int screenBrightness)`：添加新的数据点
- `clearData()`：清除所有数据点
- `setMaxDataPoints(int maxPoints)`：设置最大数据点数量
- `paintEvent(QPaintEvent *event)`：绘制曲线图

**绘图组件**：
- 背景网格线（每20%一条水平线）
- 坐标轴和刻度标签
- 双曲线（摄像头亮度和屏幕亮度）
- 图例（说明曲线颜色和含义）

### AutoBrightness 类更新

**新增成员变量**：
- `double m_gain`：曝光增益值

**新增方法**：
- `void setGain(double gain)`：设置曝光增益
- `double getGain() const`：获取当前增益值

**更新方法**：
- `openCap()`：添加增益参数设置
- `saveSettings()` / `loadSettings()`：增益值持久化

### MainWindow 类更新

**新增成员变量**：
- `BrightnessCurveWidget* m_curveWidget`：实时曲线显示组件

**新增槽函数**：
- `on_gainSlider_valueChanged(int value)`：处理增益滑块变化
- `on_samplingFreqSlider_valueChanged(int value)`：处理采样频率滑块变化

**新增辅助方法**：
- `updateSamplingFreqLabel(double freqHz)`：更新采样频率显示标签
- `updateExposureLimits(double freqHz)`：检查曝光时间与采样频率的兼容性

## 使用说明

### 启动和查看曲线

1. 点击"开始"按钮启动自动亮度调节
2. 实时亮度曲线会在"实时亮度曲线"区域自动显示
3. 曲线会随着每次采样更新，从右侧滚动

### 调整采样频率

1. 使用"采样频率"滑块调整采样频率
2. 滑块使用对数刻度，可精确控制从 0.01Hz 到 60Hz 的范围
3. 频率越高，曲线更新越频繁，但可能增加系统负载
4. 频率越低，曲线更新越慢，但降低系统资源占用

**推荐设置**：
- 正常使用：0.1Hz - 1Hz（每1-10秒采样一次）
- 快速响应：1Hz - 10Hz（每0.1-1秒采样一次）
- 测试调试：10Hz - 60Hz（每秒采样多次）

### 调整曝光增益

1. 使用"曝光增益"滑块调整增益值（0-255）
2. 增益越高，摄像头对光线越敏感
3. 在环境光较暗时适当提高增益
4. 在环境光充足时保持较低增益以避免过曝

**注意事项**：
- 过高的增益可能导致图像噪点增加
- 建议先调整曝光时间，再调整增益

### 停止和清除曲线

1. 点击"停止"按钮停止自动亮度调节
2. 曲线数据会自动清除，准备下次使用

## 配置持久化

所有新增的参数设置（增益值、采样频率）都会自动保存到配置文件中，下次启动时自动加载。

**配置文件位置**：
- Windows: `HKEY_CURRENT_USER\Software\AutoBrightnessWidget\Settings`（注册表）
- 或应用程序数据目录中的 INI 文件

## 测试

新增测试文件：`tests/test_realtime_curve.cpp`

**测试覆盖**：
- 增益滑块存在性和范围测试
- 采样频率滑块存在性和范围测试
- 曲线组框存在性测试
- BrightnessCurveWidget 基本功能测试
- BrightnessCurveWidget 最大数据点限制测试
- AutoBrightness 增益控制测试

## 未来改进方向

1. **曲线交互**：支持鼠标悬停显示具体数值
2. **数据导出**：支持导出曲线数据为CSV文件
3. **多曲线对比**：支持显示多个显示器的亮度曲线
4. **自动曝光优化**：根据环境光自动调整增益和曝光时间
5. **曲线缩放和平移**：支持用户缩放和平移曲线视图
